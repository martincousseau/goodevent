doctype html
html(lang='en')
  head
    title= title
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(
      rel="stylesheet" 
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css" 
      integrity="sha384-zCbKRCUGaJDkqS1kPbPd7TveP5iyJE0EjAuZQTgFLD2ylzuqKfdKlfG/eSrtxUkn" 
      crossorigin="anonymous"
    )
    style.
      /* Navbar styling */
      .navbar {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 1rem 2rem;
        background-color: #f8f9fa;
        border-bottom: 2px solid #e0e0e0;
      }
      .navbar-brand {
        font-size: 1.5rem;
        font-weight: bold;
        color: #333;
        text-decoration: none;
      }
      .navbar-links {
        display: flex;
        align-items: center;
        gap: 1rem;
      }
      .navbar-links a {
        padding: 0.5rem 2rem;
        border: none;
        border-radius: 4px;
        background-color: grey;
        color: white;
        text-decoration: none;
        font-size: 0.9rem;
        transition: background-color 0.3s;
        margin-right: 0.2em; 
      }
      .navbar-links a:hover {
        background-color: #0056b3;
      }

      /* Centering main content */
      .main-content {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 80vh;
      }

  body
    div.container-fluid
      nav.navbar
        a.navbar-brand(href='/home') Good Event
        div.navbar-links
          if user
            a(href="/event") Créer un évènement
            a(href="/create-conversation") Créer une conversation
            form.form-inline(method='POST' action="/logout")
              a(href="/account") Mon compte
              button.btn.btn-primary(type="submit") Déconnexion

    div.main-content
      <br>
      <br>
      h1= title
      block content
